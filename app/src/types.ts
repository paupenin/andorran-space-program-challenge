// Not the most elegant solution, but it works for a challenge :)
import {
  Location,
  Variant,
  Item,
  ObjectId,
  ItemList,
} from "../../api/src/types";

export type { Location, Variant, Item, ObjectId, ItemList };
